{% set name = "click-help-colors" %}
{% set version = "0.7" %}
{% set sha256 = "fa574d591ee1d80e6da035df9f471cf0248060c177ff8f99008e45b8401c7fe2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - pip
    - python
    - setuptools

  run:
    - python
    - click

test:
  imports:
    - click_help_colors

about:
  home: https://github.com/r-m-n/click-help-colors
  license: MIT
  license_file: LICENSE.txt
  # License file missing on pypi. See: https://github.com/r-m-n/click-help-colors/issues/2
  #license_file: LICENSE.txt
  summary: Colorization of help messages in Click
  dev_url: https://github.com/r-m-n/click-help-colors
  license_family: MIT

extra:
  recipe-maintainers:
    - saketkc
